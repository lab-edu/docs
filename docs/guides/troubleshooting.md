# 故障排查

这一页记录最常见的验证点，便于团队在联调失败时快速定位问题。

## 先看什么

- core 的 `/actuator/health` 是否返回正常
- web 是否能通过同源入口请求到 core
- nginx 是否正确转发 `/api/` 和页面请求
- 数据库容器是否已就绪

## 常见方向

- 启动失败优先看 Docker 和 Compose 日志
- 接口失败优先看后端日志和响应码
- 页面空白优先看前端构建结果与浏览器控制台

## 排查原则

- 先确认链路是否通，再看具体业务逻辑
- 先复现，再修复，再回归
- 用最小假设定位问题，不要一次改太多变量